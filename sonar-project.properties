# Required metadata
sonar.projectKey=Xml
sonar.projectName=Xml

# Comma-separated paths to directories with sources (required)
sonar.sources=src/

# Language
sonar.language=php

# Encoding of sources files
sonar.sourceEncoding=UTF-8
